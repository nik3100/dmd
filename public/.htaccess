<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /dmd/public/

    # Redirect to front controller if request is not a real file or directory
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]
</IfModule>
